# Grafana dashboard provisioning configuration

apiVersion: 1

providers:
  - name: 'gecko-dashboards'
    orgId: 1
    folder: 'GeckoTerminal Collector'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json